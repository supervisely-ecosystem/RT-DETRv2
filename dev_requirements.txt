# Resume-upload feature (unreleased) — pin to the SDK branch until it is merged & released,
# then switch back to a pinned version (supervisely[...]==<version>).
supervisely[training,tracking,model-benchmark] @ git+https://github.com/supervisely/supervisely.git@feat/train-resume-upload

setuptools==69.5.1

torch==2.7.1
torchvision==0.22.1
onnx==1.16.1
onnxruntime-gpu==1.19.2
tensorrt-cu12==10.9.0.34

tensorboard

imgaug==0.4.0
plotly==5.22.0
